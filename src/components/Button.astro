---
import { Image } from "astro:assets";

const { src, title, alt, loading } = Astro.props;
---

<Image
  decoding="async"
  loading={loading}
  src={src}
  title={title}
  alt={alt}
  height="31"
  width="88"
/>
<style>
  img {
    image-rendering: pixelated;
  }
</style>
