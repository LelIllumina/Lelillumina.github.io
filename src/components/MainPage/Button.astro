---
import { Image } from "astro:assets";

const { src, title, alt, loading } = Astro.props;
---

<Image
  decoding="async"
  loading={loading}
  src={src}
  title={title}
  alt={alt}
  height="31"
  width="88"
/>
<style>
  img {
    margin: auto 0;
    image-rendering: pixelated;
  }
</style>
